import React from "react";

interface MediaDemoPageProps { }

const MediaDemoPage = (props: MediaDemoPageProps) => {
    return <div>Media demo page here</div>;
};

MediaDemoPage.displayName = "MediaDemoPage";

export default MediaDemoPage;
