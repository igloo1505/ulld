---
category: Referencing
component: EqRef
id: eqref
aliases: []
---

## Interfaces

### EquationRefProps


The EqRef component is used in two related but distinct ways. An equation can be wrapped in a EqRef component with the anchor property and an id property to gather the equation number in a separate EqRef component, with the same id but without the anchor property.

#### Example

```mdx
<EqRef anchor id="deltaGammaEquivalence">
  $$ \gamma = \frac{1}{\sqrt{1 - \frac{v^{2}}{c^{2}}}} \equiv 1 + \frac{1}{R} \int_{0}^{R} 2 G \frac{M}{R^{3}} $$
</EqRef>
Consider that in equation <EqRef id="deltaGammaEquivalence" />
...
```

### Output

$$
\gamma = \frac{1}{\sqrt{1 - \frac{v^{2}}{c^{2}}}} \equiv 1 + \frac{1}{R} \int_{0}^{R} 2 G \frac{M}{R^{3}}
$$

Consider that in equation 1...

#### Properties

| Property          | Type                 | Description |
| ----------------- | -------------------- | ----------- |
| `id`              | `string`             | **`Alpha`** |
| `children`        | `ReactNode`          | **`Alpha`** |
| `anchor?`         | `boolean`            | **`Alpha`** |
| `defaultContent?` | `string` \| `number` | **`Alpha`** |

