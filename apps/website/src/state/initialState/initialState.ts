import { initialCoreState } from "./core";
import { initialInteractionsState } from "./interactions";

export const initialState = {
    core: initialCoreState,
    interactions: initialInteractionsState,
};
