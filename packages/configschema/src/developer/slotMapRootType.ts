export type SlotMap = {
    UI: {
        confirmationModal: string,
        landingPage: string,
    }, 
    navigation: {
        secondary: string,
        navbar: string,
    }, 
    commandPalette: {
        commandPalette: string,
    }, 
}
