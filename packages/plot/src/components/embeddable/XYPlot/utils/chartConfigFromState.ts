import { XYPlotState } from "../context";
import { ChartConfigType } from "../types";

export const getChartConfigFromState = (state: XYPlotState): ChartConfigType => {
    let x: ChartConfigType = {
    }
    return x
}
