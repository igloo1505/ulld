import { CalculatedPlotParams, XYPlotItem } from "../types";
import { ParsedLineProps } from "./schemas";

export const linePlotPropsToPlotItem = (props: ParsedLineProps, additionalProps: CalculatedPlotParams): XYPlotItem => {
     
}
