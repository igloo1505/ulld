import { XYPlotProps } from "../../types";
import { ChartConfig } from "./shadChart";

export const getShadChartXYConfig = (data: XYPlotProps): ChartConfig => {
    let config: ChartConfig = {

    }
    return config
}
