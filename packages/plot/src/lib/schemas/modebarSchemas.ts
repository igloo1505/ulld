import {z} from 'zod'

export const modeBarBtn = z.union([
    z.literal("autoScale2d"),
    z.literal("autoscale"),
    z.literal("editInChartStudio"),
    z.literal("editinchartstudio"),
    z.literal("hoverCompareCartesian"),
    z.literal("hovercompare"),
    z.literal("lasso"),
    z.literal("lasso2d"),
    z.literal("orbitRotation"),
    z.literal("orbitrotation"),
    z.literal("pan"),
    z.literal("pan2d"),
    z.literal("pan3d"),
    z.literal("reset"),
    z.literal("resetCameraDefault3d"),
    z.literal("resetCameraLastSave3d"),
    z.literal("resetGeo"),
    z.literal("resetSankeyGroup"),
    z.literal("resetScale2d"),
    z.literal("resetViewMapbox"),
    z.literal("resetViews"),
    z.literal("resetcameradefault"),
    z.literal("resetcameralastsave"),
    z.literal("resetsankeygroup"),
    z.literal("resetscale"),
    z.literal("resetview"),
    z.literal("resetviews"),
    z.literal("select"),
    z.literal("select2d"),
    z.literal("sendDataToCloud"),
    z.literal("senddatatocloud"),
    z.literal("tableRotation"),
    z.literal("tablerotation"),
    z.literal("toImage"),
    z.literal("toggleHover"),
    z.literal("toggleSpikelines"),
    z.literal("togglehover"),
    z.literal("togglespikelines"),
    z.literal("toimage"),
    z.literal("zoom"),
    z.literal("zoom2d"),
    z.literal("zoom3d"),
    z.literal("zoomIn2d"),
    z.literal("zoomInGeo"),
    z.literal("zoomInMapbox"),
    z.literal("zoomOut2d"),
    z.literal("zoomOutGeo"),
    z.literal("zoomOutMapbox"),
    z.literal("zoomin"),
    z.literal("zoomout"),
]);


export const modeBarBtnRemove = z.union([
    z.literal("autoScale2d"),
    z.literal("autoscale"),
    z.literal("editInChartStudio"),
    z.literal("editinchartstudio"),
    z.literal("hoverCompareCartesian"),
    z.literal("hovercompare"),
    z.literal("lasso"),
    z.literal("lasso2d"),
    z.literal("orbitRotation"),
    z.literal("orbitrotation"),
    z.literal("pan"),
    z.literal("pan2d"),
    z.literal("pan3d"),
    z.literal("reset"),
    z.literal("resetCameraDefault3d"),
    z.literal("resetCameraLastSave3d"),
    z.literal("resetGeo"),
    z.literal("resetSankeyGroup"),
    z.literal("resetScale2d"),
    z.literal("resetViewMapbox"),
    z.literal("resetViews"),
    z.literal("resetcameradefault"),
    z.literal("resetcameralastsave"),
    z.literal("resetsankeygroup"),
    z.literal("resetscale"),
    z.literal("resetview"),
    z.literal("resetviews"),
    z.literal("select"),
    z.literal("select2d"),
    z.literal("sendDataToCloud"),
    z.literal("senddatatocloud"),
    z.literal("tableRotation"),
    z.literal("tablerotation"),
    z.literal("toImage"),
    z.literal("toggleHover"),
    z.literal("toggleSpikelines"),
    z.literal("togglehover"),
    z.literal("togglespikelines"),
    z.literal("toimage"),
    z.literal("zoom"),
    z.literal("zoom2d"),
    z.literal("zoom3d"),
    z.literal("zoomIn2d"),
    z.literal("zoomInGeo"),
    z.literal("zoomInMapbox"),
    z.literal("zoomOut2d"),
    z.literal("zoomOutGeo"),
    z.literal("zoomOutMapbox"),
    z.literal("zoomin"),
    z.literal("zoomout"),
]);
