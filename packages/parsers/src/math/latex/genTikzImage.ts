// TODO: Come back here nad handle tikz stuff here. The latex -> svg parser is **incredible**. Should be able to handle this if I understand the build process properly.
// import { parseLatex } from "./parseLatex"

// export const genTikzImage = async (tikz: string) => {
//     return await parseLatex(`\\documentclass{standalone}
// \\usepackage{color,xcolor}
// \\usepackage{tikz-qtree, tikz}
// \\usetikzlibrary{decorations.pathreplacing,arrows,shapes,positioning,shadows,calc}
// \\begin{document}
// ${tikz}
// \\end{document}`)
// }
