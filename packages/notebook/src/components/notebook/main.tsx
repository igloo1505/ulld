import React from "react";
import { NotebookProps } from "../../types";

interface UlldNotebookProps extends NotebookProps { }

const UlldNotebook = (props: UlldNotebookProps) => {
    return <div className={"text-8xl w-full text-center"}>FIX ME</div>;
};

UlldNotebook.displayName = "UlldNotebook";

export default UlldNotebook;
